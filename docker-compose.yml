version: '3.8'

services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: orderx-app

    ports:
      - "5000:5000"

    environment:
      # Server
      NODE_ENV: ${NODE_ENV:-production}
      PORT: ${PORT:-5000}
      
      # Database - MongoDB Atlas
      DATABASE_URL: "mongodb+srv://order-x:yh8xBDpKe61xJRES@cluster0.hyv0wnb.mongodb.net/order-x?retryWrites=true&w=majority"

      # JWT
      JWT_SECRET: secret
      JWT_EXPIRES_IN: 7d
      
      # Stripe
      STRIPE_SECRET_KEY: sk_test_51OBfrIDITPfpX7OJWEZmGoDSyNxy9B0TFhOmwFq7tRSfPFhjorLcSY2EGS5tBtZDcpGagufd74t4IGjDxJmOhUDu00yiDOru7X
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET}

      # HuggingFace
      HUGGINGFACE_API_KEY: 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
      
      # Frontend
      FRONTEND_URL: http://localhost:3000

    restart: unless-stopped

    networks:
      - orderx-network

networks:
  orderx-network:
    driver: bridge
